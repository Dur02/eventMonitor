export interface configSettingInitialValueType {
  configName: string,
  configType: string[] | null,
  remark: string | null,
  orderPriority: number | null,
  purview: number | null,
  isMonitor: number | null
}